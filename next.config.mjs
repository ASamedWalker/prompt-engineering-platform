// Import the necessary types from Next.js
// Note: Type imports are not needed in JavaScript

/** @type {import('next').NextConfig} */
const nextConfig = {
  // config options here
};

export default nextConfig;